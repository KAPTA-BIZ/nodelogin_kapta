<!DOCTYPE html>
<html lang="en">
<head>
	<title>Authentication with Node</title>
	<% include partials/head %>
</head>
<body>

	<div class="container">
		<div class="card text-center mt-5">
			<div class="card-body">
                <img width="100px" src="https://pbs.twimg.com/profile_images/836695399809302528/568l42NJ_400x400.jpg">
				<h1>
				<span class="fa fa-block"></span>LISTA USUARIOS</h1>
				<table class="table">
				  
                        <thead>
                          <tr>
                            <th>#</th>
                            <th>Email</th>
                            <th>Pass</th>
                            <th>Opción</th>
                          </tr>
                        </thead>
                        <tbody>
                          <% items.forEach(function(item) { %> 
                          <tr>
                            <% if(item.local) {%>
                            <!-- Muestra solo instructores -->
                              <% if(!item.sa){ %>
                                <td><%= item._id %></td>
                                <td><%= item.local.email%></td>
                                <td><%= item.local.password %></td>
                                <td><a href=""><button>Asociar Link</button></a></td>
                          </tr>
                          <% }}}); %>
                        </tbody>
          </table>
			</div>
		</div>
	</div>

</body>
</html>
